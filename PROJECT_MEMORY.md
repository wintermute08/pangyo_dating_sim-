# 프로젝트 메모리

최종 갱신: 2026-09-07 UTC
저장소: `wintermute08/pangyo_dating_sim-`

이 문서는 세션이 바뀌어도 현재 결정과 작업 상태를 그대로 이어가기 위한 인수인계 기록이다. 구현과 충돌할 경우 최신 코드와 사용자의 직접 지시를 우선한다.

## 2026-09-07 대화 UI 정리

- 사용자 승인: 거대한 흰 대화창을 줄이고 학원 비주얼노벨 분위기로 정리한다.
  아래 과거 UI 설명과 충돌하면 이 항목을 우선한다.
- 대화창: 겹친 흰 배경(합성 불투명도 약 99%)을 단일 82% 배경으로 변경.
  데스크톱 최소 높이는 `clamp(148px, 19svh, 218px)`, 긴 대사는 자연스럽게
  늘어난다. 상하 여백을 줄이고 본문은 20~28px, 줄간격 1.65로 조정했다.
- 이름표: 최소 너비 180px를 없애고 이름 길이에 맞는 작고 각진 탭으로 변경.
- 저장·불러오기·로그·자동·설정·타이틀은 대화창 밖 `#game-menu`로 분리.
  데스크톱은 하단 한 줄, 폭 900px 이하 또는 높이 620px 이하에서는 접는 메뉴.
  모바일 버튼 높이는 44px이며 메뉴를 열면 자동 진행을 일시정지한다.
  Escape·바깥 클릭·항목 선택·반응형 분기 변경 시 닫힌다.
- 장면 번호·날짜·제목은 작은 한 줄로, 모바일은 영문 부가 정보를 숨긴다.
  대화창·선택지·진행도는 파랑/네이비로 통일하고 꽃잎의 분홍은 유지했다.
- 배경 6종은 기준 화면 블러 4px → 약 1.5px, 밝기 .965 → .985로 재생성.
  `tools/soften_bg.py`도 같은 목표값으로 갱신했다. 이번 환경에 Pillow가 없어
  임시 설치한 Sharp로 동일 크기·블러·밝기 파라미터를 적용했다. 인코더가
  달라 Python 재생성 결과와 파일 바이트는 일치하지 않을 수 있다.
- 햇빛 오버레이 강도를 줄이고 인물 채도에 `.96` 보정을 추가했다.
  인물 크기·컷별 위치와 스프라이트 전환 구조는 유지했다.
- 검증: 기존 스프라이트 테스트 9개 통과. 임시 jsdom 검증으로 메뉴 분리,
  자동 진행 일시정지/재개, Escape, 바깥 클릭, 분기 변경, 키보드 조작,
  저장/불러오기/로그/설정/타이틀 연결을 확인했다. CSS 파싱·JS 문법 검사 통과.
  연결된 브라우저가 없어 실제 렌더링과 모바일 프레임 성능은 미검증이다.

## 2026-09-07 스프라이트 전환 수정

- `js/sprite-transition.js`가 이미지 준비와 전환을 담당한다. 아래의 과거
  `characterTimer`/투명도 초기화 설명보다 이 구현을 우선한다.
- 빠르게 다음 표정을 요청해도 진행 중인 320ms 전환을 끊지 않는다. 대기 요청은
  최신 표정 하나만 남긴다. 같은 표정의 재요청은 전환을 다시 시작하지 않는다.
- 다운로드와 디코딩을 끝낼 때까지 이전 컷을 유지한다. 타이틀 복귀·불러오기·루트
  변경 때에는 세대 번호로 이전 비동기 작업을 무효화한다.
- 일반 알파 합성은 두 컷이 각각 50%일 때 합성 불투명도가 75%로 내려간다.
  `mix-blend-mode: plus-lighter`와 부모의 `isolation: isolate`로 두 컷의
  불투명도를 더해 전환 중 배경이 비치는 현상을 막는다.
- 장면 퇴장은 부모의 투명도로 처리하여 진행 중인 두 컷의 합성을 유지한다.
- 검증: `node tests/sprite-transition.test.cjs` 9개 통과. 연결된 브라우저가
  없어 이번 수정의 실제 렌더링과 프레임 성능은 아직 검증하지 않았다.

## 사용자 결정

- 작품은 판교고를 배경으로 하는 약 10분 분량의 학원 로맨스 비주얼노벨이다.
- 제목은 **판교고 미연시**, 부제는 **봄은, 열 걸음 늦게 온다**다.
- 히로인 이름은 **판교하**다. `한서윤`, `서윤`이라는 옛 이름을 다시 사용하지 않는다.
- **성별 선택 루트.** 시작할 때 플레이어 성별을 묻고 상대역이 갈린다.
  남학생이면 **판교하**(여), 여학생이면 **판교준**(남). 이름·스탠딩·손위
  형제 호칭만 갈리고 나머지 대사는 공유한다 — 반말 대화라 대부분 중립이다.
  상대역 데이터는 `js/game.js` 의 `PARTNERS` 한 곳에 있다.
- 시나리오는 이름을 직접 쓰지 않고 토큰을 쓴다.
  `{her}`(판교하/판교준) · `{her_s:조사}`(교하는/교준은) · `{sis}`(언니/누나).
  조사는 받침에 맞춰 자동으로 고른다. 이름만 바꾸면 조사가 틀리기 때문이다.
- 플레이어 이름은 **게임이 제안하지 않는다**. 시작할 때 반드시 직접 입력한다.
  기본값을 채워 두면 그대로 넘어가게 되고, 그 이름이 실제 같은 학교 학생과
  겹칠 수 있어서다. 저장 데이터에 이름이 없을 때만 자리표시자 `주인공`을 쓴다.
- 초기 `판교고 10분 미연시 전체 제작 기획서`의 참고 비중은 낮춘다. 최종 UI 목업과 사용자의 최신 지시를 우선한다.
- 그래픽의 현재 화풍과 전체적인 색감은 사용자가 마음에 들어 한다. 이후 수정에서도 기존 에셋과 봄·하늘색·연분홍·네이비 톤을 유지한다.
- 효과는 화면이 비어 보이지 않을 정도로만 사용하고 과하게 만들지 않는다.
- 모바일 최적화와 자연스러운 인물 전환이 중요하다.

## 현재 구현

- 별도 빌드가 필요 없는 정적 웹 게임이다. 루트의 `index.html`을 정적 서버로 제공하면 실행된다.
- 주요 파일:
  - `index.html`: 전체 화면과 모달 구조
  - `css/game.css`: 데스크톱·모바일 UI, 화면 효과, 인물 배치
  - `js/game.js`: 게임 진행, 저장, 사운드, 애니메이션과 UI 동작
  - `script/story.js`: 7개 장면(체육시간 포함), 5개 선택지
  - `script/ending.js`: 호감도와 마지막 선택에 따른 3종 엔딩
- 구현 기능:
  - 이름 입력
  - 6개 장면과 3개 엔딩
  - 자동 저장, 수동 저장 6슬롯, 불러오기
  - 대화 로그, CG 갤러리, 환경설정
  - 텍스트 속도·자동 진행·BGM·효과음 조절
  - Web Audio 기반 합성 BGM·효과음
  - 키보드 조작, 전체 화면, 모션 감소 설정
  - 반응형 모바일 세로·가로 레이아웃

## 그래픽과 타이틀

- 실제 게임 에셋은 `assets/`에 있다.
- 전달받은 원본과 비교 자료는 `docs/reference/`에 정리했다.
- 151MB짜리 `docs/reference/ui-mockup-handoff.zip`은 로컬에 보존하지만 `.gitignore`로 Git 추적에서 제외한다.
- 현재 타이틀은 `assets/title-logo-v2.png`다.
- 타이틀 v2는 기존 타이틀 참고 이미지를 생성 입력으로 사용하지 않은 독립적인 이미지 생성 결과다.
- 타이틀 방향: 현대적인 학교 노트 편집 디자인, 네이비 잉크, 하늘색 선, 열 걸음 발자국, 꽃잎 하나.
- 참고 이미지와 유사도가 높았던 첫 타이틀은 `docs/reference/title-logo-generated-v1.png`로 보관한다.
- HTML 텍스트 제목 대신 PNG 타이틀을 타이틀 화면과 스토리 타이틀 카드에서 사용한다.

## 모바일·인물 연출 상태

- 최신 관련 커밋: `577ede7` (`모바일 연출과 캐릭터 전환 개선`)
- 모바일에서 노치와 홈바 안전 영역을 `env(safe-area-inset-*)`로 반영했다.
- 모바일 대화창, 선택지, 모달, 상단 HUD, 빠른 설정 버튼의 크기와 간격을 별도 보정했다.
- 터치 버튼에는 모바일 탭 동작을 적용했고 터치 환경에서 고정되는 hover 이동을 막았다.
- 인물의 수평 중심축은 화면 방향에 따라 다르다. **세로에서는 기기와 무관하게 50%**, 가로와 데스크톱은 76%다.
  - 가로 배치(인물 오른쪽 + 왼쪽 여백에 대사)를 세로에 그대로 쓰면 인물이 오른쪽으로 잘리고 왼쪽만 크게 빈다.
    적용 전 실측: iPhone 14 중심 67%·오른쪽 64px 잘림, iPad 72%·왼쪽 319px 공백, iPad Pro 76%·왼쪽 505px 공백.
  - 세로에서는 대사창이 화면 폭을 전부 쓰므로 인물을 오른쪽으로 밀 이유가 없다.
  - 이전 값(세로 67% / 태블릿 72%)은 더 쓰지 않는다.
- 세로 분기는 `@media (orientation: portrait)` 세 단계다. 공통(수평 위치), 621px 이상 태블릿(인물 확대 + 대사 도구 균등 배치),
  901px 이상 iPad Pro급(모바일·태블릿 분기에 걸리지 않고 데스크톱 규칙을 받으므로 별도 지정).
- 전신 컷은 화면 바닥에 세우고 대화창이 다리를 덮는다.
- 허리·바스트 컷은 이미지 내 얼굴 위치가 전신 컷과 맞도록 각각 위로 들어 올린다.
- 인물 컷의 투명 캔버스 여백이 서로 크게 다르므로 단순한 동일 `right` 값으로 되돌리지 않는다.
- 표정 변경에는 `#character-ghost`와 `#character` 두 이미지를 겹치는 약 0.52초 크로스페이드를 사용한다.
- **컷 교체는 순수 크로스페이드다. 위치를 움직이지 않는다.**
  - 키프레임에서 `transform` 이동과 `scale`을 뺐다. 이전에는 들어오는 컷이 `(12px, 6px)`, 나가는 컷이 `(-8px, -2px)`로 이동해 두 이미지가 20px 어긋난 채 겹쳤다.
  - 교체 중에는 `.character.is-swapping`으로 `left`/`bottom`/`height` 트랜지션을 끈다. 컷마다 `bottom`·`height`가 달라 켜 두면 새 컷이 0.45초 동안 제자리로 미끄러져 들어온다.
  - `is-swapping`은 `src`와 `data-look`을 바꾸기 **전에** 붙여야 한다. 뒤에 붙이면 이미 트랜지션이 시작된 뒤라 소용이 없다.
  - 실측: 수정 전 데스크톱 가로 14.7px·세로 109.3px·높이 52.8px 이탈 → 수정 후 전 축 0.0px. 크로스페이드 자체는 유지된다.
  - 최초 등장 애니메이션(`character-in`)은 그대로 두었다. 등장은 아래에서 올라오는 연출이 의도된 것이다.
- 최초 등장 애니메이션과 표정 교체 애니메이션은 분리돼 있다.
- 화면 효과는 느린 햇빛 번짐, 소수의 부유 광점, 꽃잎, 모바일 배경 드리프트로 구성한다.
- 모션 감소 설정에서는 꽃잎과 광점을 숨기고 애니메이션을 사실상 비활성화한다.

## 체육시간 씬 (04)

- 씬 `04 숨을 고르는 동안`(FIFTH PERIOD)을 03과 기존 04 사이에 넣었다. 전체 7씬이 되었고 뒤 씬 번호가 하나씩 밀렸다.
- 선택지는 **호감도 0의 취향 선택지**다. 자판기 선택지와 같은 성격이며, 기존 엔딩 밸런스(도달 경로 81개 · good 12 / normal 60 / other 9)를 건드리지 않는다.
- 스탠딩 `hoodie_calm` = `assets/ch_stand_hoodie_calm.png`.
  - 실제 판교고 PGHS 회색 후드집업 사진과 기존 `ch_stand_calm.png`를 함께 레퍼런스로 넣어 **의상만** 바꾼 컷이다. 얼굴·머리·머리핀·비율·포즈가 다른 컷과 동일하다.
  - 생성 결과는 흰 배경 RGB였다. 다른 스탠딩과 맞추려고 테두리 flood fill로 배경을 빼고 RGBA로 저장했다. 인물 안쪽 흰색(티셔츠·드로우코드·양말)은 테두리와 이어져 있지 않아 그대로 남는다.
  - **테두리 flood fill 만으로는 머리카락에 둘러싸인 배경 조각이 남는다.** 옆머리와 어깨 사이의 얇은 흰 띠가 그랬다. 두 기준을 함께 써서 따로 지웠다.
    1. 조각 바깥 한 겹이 대부분 어두운가 → 머리카락에 둘러싸였다
    2. 실루엣 바깥(투명)까지 거리가 70px 미만인가 → 가장자리의 얇은 틈이다
    드로우코드도 1번은 만족하지만(어두운 외곽선과 초록 글자에 접함) 몸 한가운데라 2번에서 걸러진다. 실측: 머리 틈 78%/4px, 드로우코드 64~66%/101~195px.
    조건 하나만 쓰면 드로우코드가 지워진다. 반드시 둘을 같이 봐야 한다.
  - 프레이밍 검증: 가로중심 49.3%(기존 48.6%), 머리 위 여백 4.9%(기존과 동일).
  - 명찰은 `판교하`다. 실물 사진의 명찰은 실존 인물 이름이라 쓰지 않는다.
  - **생성 모델이 명찰 한글을 `만고하`로 잘못 그렸다.** 스프라이트를 다시 생성하면 얼굴이 달라지므로 `tools/relabel_name_patch.py`로 명찰 안쪽 글자만 덮어썼다. 테두리와 알파는 건드리지 않는다. 변경 범위는 1,368~1,485px 이고 알파 변경은 0px 이다.
  - 후처리 스크립트는 `tools/`에 있다. 앞으로 후드 컷을 다시 만들면 같은 두 단계를 거쳐야 한다.
  - 신발은 흰 실내화다. 처음 생성물은 교복 로퍼였는데 체육관 바닥과 맞지 않아 같은 컷을 기준 이미지로 다시 넣어 신발만 바꿨다.
- 스탠딩 `hoodie_smile` = `assets/ch_stand_hoodie_smile.png`.
  - `hoodie_calm`(신발 수정본)을 기준 이미지로 삼아 **표정만** 바꿨다. 정렬 오차는 최대 18px(세로 2752px 기준 0.65%)이라 크로스페이드에서 어긋나 보이지 않는다.
  - 두 컷 모두 `crop: 'full'`로 통일했다. 프레이밍이 같아야 컷 교체 시 위치가 흔들리지 않는다.
  - 씬 04의 표정 흐름: calm → calm → smile → calm → smile → smile → calm → smile.
- 배경 `bg06_gym_afternoon.png`는 **임시 생성본**이다. 사용자가 실제 체육관을 촬영해 AI 변환한 이미지로 교체할 예정이다. 파일명을 그대로 덮어쓰면 된다.

## 인물 상시 연출 (기울임·그림자)

숨쉬기·말하기(`is-speaking`)·감정 반응(`reacts-*`)은 전부 뺐다. 배경이 바뀐 뒤
인물이 위아래로 움직이는 게 거슬린다는 제보의 원인이 감정 반응이었다.
`playReaction` 은 크로스페이드가 끝난 480ms 타이머 안에서 돌아 전환 직후에
튀었다. 제거 후 대기 중 이동량 70건/52.7px -> 0건/0.0px.

- **기울임은 `#character-stage` 래퍼에 건다.** 인물과 잔상을 함께 감싸므로 컷
  교체 중에도 두 장이 같은 위상으로 기운다. 인물 요소에 직접 걸면 잔상이
  `hidden` 이었다가 다시 나타날 때 애니메이션이 처음부터 시작해 위상이
  어긋나고, 크로스페이드 중 두 그림이 최대 0.36도 벌어져 겹쳐 보인다.
- **발끝을 축으로 한 통짜 회전 0.18도, 주기 5.3초.** 관절 4개(발목/골반/가슴/목)를
  각각 다른 각도로 돌리는 체인을 먼저 만들어 비교했는데, 이 진폭에서는 통짜
  회전과 화면상 최대 0.44px 차이밖에 안 났다(전체 이동량 2.7px 의 16%, 원본
  픽셀 기준 최대 차이 255 중 12). 체인은 구간마다 스프라이트 사본이 필요하고
  사본마다 `drop-shadow` 를 다시 계산하므로 쓰지 않는다.
- **회전축은 `--char-x`.** `.character` 의 `left` 와 같은 값을 봐야 한다. 어긋나면
  그만큼 발끝이 원호를 그리며 들린다. 세로 화면에서는 인물이 50% 로 가므로
  축도 함께 옮긴다.
- 변형 속성 분담이 정해져 있다. 겹치면 서로를 덮어쓴다.

  | 속성 | 용도 |
  | --- | --- |
  | `translate` | 가운데 정렬 (`-50% 0`) |
  | `transform` (인라인) | 등장 연출 |
  | 래퍼의 `rotate` | 기울임 |

  기울임에 `transform` 이 아니라 `rotate` 속성을 쓰는 이유가 이것이다. 등장
  연출이 인라인으로 `transform` 을 잡았다 놨다 하므로 같은 속성을 쓰면 끊긴다.

- **그림자는 방향성 한 겹.** `drop-shadow(-9px 20px 26px rgba(22,32,58,.34))`.
  예전의 `0 20px 28px .22` 는 균일한 후광이라 인물이 배경 위에 붙어 보였다.
  겹을 늘리면 안 된다 — 두 겹은 예전에 재서 뺀 것이다(50ms 초과 41% -> 5%).
- 기울임 실측(Chromium, 게임 실행 상태에서 표식을 심어 130회 샘플링):

  | | 데스크탑 | 모바일 세로 | 태블릿 세로 |
  | --- | --- | --- | --- |
  | 회전각 범위 | 0.360도 | 0.360도 | 0.360도 |
  | 발끝 가로/세로 | 0.00px | 0.00px | 0.00px |
  | 머리 세로 | 0.00px | 0.00px | 0.00px |
  | 머리 가로 | 4.52px | 4.35px | 6.01px |

  세로 이동이 모든 화면에서 0 이다. 프레임 시간은 중앙값 34ms 로 켜고 끈 것이
  같고, 차이는 반복 측정 편차(5~10%) 안에 있다.
- `reduce-motion` 에서는 `.character-stage` 의 기울임도 끈다.

## 배경 아웃포커스 (입체감)

인물과 배경이 똑같이 선명하면 같은 평면에 붙어 보인다. 배경을 아웃포커스로
날리면 인물이 앞으로 나온다.

- **CSS `filter: blur()` 를 실행 중에 걸면 안 된다.** 장면이 바뀔 때마다 화면
  전체를 다시 그린다. 실측(모바일 세로, 빠르게 넘기기, 각 조건 2회):

  | 조건 | 중앙 | 상위 5% | 50ms 초과 |
  | --- | --- | --- | --- |
  | 블러 없음 | 37 / 38ms | 68 / 64ms | 14% / 16% |
  | 블러 4px | 47 / 48ms | 79 / 62ms | 44% / 41% |
  | 블러 + `will-change` | 46 / 45ms | 63 / 79ms | 35% / 38% |
  | 블러 + `will-change` + `contain:paint` | 49 / 48ms | 89 / 63ms | 44% / 43% |

  레이어 승격으로는 해결되지 않는다. `contain: paint` 는 오히려 나빠졌다.
- **흐림을 파일에 굽는다** (`tools/soften_bg.py`). 실행 비용이 0 이 된다. 흐린
  그림에는 고주파가 없어서 절반 크기 JPEG 로 저장해도 화면에서 구분이 안
  된다(같은 장면 스크린샷 비교, 배경 영역 픽셀 차이 255 중 평균 0.91,
  95퍼센타일 2.0). 덤으로 배경 합계가 41.4MB -> 460KB 가 됐다.
- 흐림 반지름은 화면 4px 기준이다. `.stage-bg` 는 뷰포트의 104% 에
  `object-fit: cover` 라 2752x1536 원본의 화면 배율이 데스크탑 가로 0.54,
  모바일 세로 0.57 로 둘 다 세로가 기준이 된다. 그래서 원본 기준 7.2px,
  절반으로 줄인 뒤 거는 값은 3.6px.
- **선명한 원본 PNG 는 지우지 않는다.** 타이틀 화면이 `bg01` 을 그대로 쓰고
  (`index.html`), 갤러리가 `GALLERY_ASSETS` 로 선명한 쪽을 쓰며, 흐림 정도를
  바꾸려면 원본에서 다시 구워야 한다.
- **갤러리는 `GALLERY_ASSETS` 를 먼저 본다.** 무대용 흐린 배경을 갤러리에 쓰면
  열어 놓은 항목이 흐리게 나오는데, 잠긴 항목에 이미 `blur(10px)` 가 걸려
  있어서 열린 것과 잠긴 것이 구분되지 않는다. 미리 받기 목록에는 넣지 않아
  갤러리를 열 때만 받는다.
- CG(`cg01`~`cg03`)는 클로즈업이라 선명한 원본을 그대로 쓴다.

## 인물이 튀던 문제 (원인 세 가지)

빠르게 넘길 때 인물이 튄다는 제보로 파고들어 세 가지가 나왔다.

### 1. 스탠딩마다 원본 비율이 달라 박스 폭이 바뀐다 — 진짜 위치 점프

- `ch_bust_calm.png` 와 `ch_waist_blush.png` 만 **1696x2528(0.671)** 이고 나머지는 **1536x2752(0.558)** 이다.
- `.character` 가 `width: auto` 라 폭이 "높이 x 그림 비율" 로 정해진다. 가운데 정렬이라 폭이 바뀌면 인물이 옆으로 움직인다.
- 실측: 같은 waist 크롭인데 `waist_smile → waist_blush` 에서 폭 422→507px, x 42px 이동.
- **`.character` 에 `aspect-ratio: 1696 / 2528` 를 줘 박스를 고정했다.** 가장 넓은 원본에 맞춰야 어떤 컷도 폭 때문에 줄지 않는다. 좁은 그림은 `object-fit: contain` 으로 높이에 맞춰 들어가고 남는 좌우는 투명이다.
- 결과: 같은 크롭 교체 시 x 이동 42px → **0px**. 그려진 인물 크기는 그대로(데스크톱 90% / 세로 82% / 가로폰 88%).
- 크롭이 다를 때(full↔waist) 18px 차이는 남는다. 원래 구도가 다른 컷이라 정상이다.
- **스탠딩을 새로 만들 때 비율을 1536x2752 로 맞추는 것이 좋다.** 지금은 CSS 로 흡수하고 있다.

### 2. animation 은 중간에 끊기면 값이 튄다

- 컷 교체와 첫 등장을 `animation` 으로 하고 있었다. 클래스를 떼는 순간 투명도가 기본값으로 되돌아간다. 빠르게 넘겨 페이드가 진행 중일 때 다음 컷이 시작되면 중간값에서 1 로 튄다. 실측 `0.245 → 1` 같은 도약이 12회.
- **둘 다 `transition` + 인라인 투명도 방식으로 바꿨다.** transition 은 현재 계산값에서 이어서 보간하므로 끊겨도 튀지 않는다.
- 끊을 때는 **지금 보이는 그림을 그 투명도 그대로 잔상으로 넘긴다**(`getComputedStyle` 로 읽어서 인라인으로 준다).
- 결과: 짧은 프레임에서의 진짜 불연속 12회 → 1회. 1.0 으로 튀는 경우는 사라졌다.
- `is-appearing` / `is-entering` / `is-leaving` / `is-swapping` 클래스와 `character-in` / `character-cross-*` 키프레임은 모두 제거했다. 대사쪽 `is-entering`/`is-leaving` 은 별개이므로 남아 있다.

### 3. 진행 중인 크로스페이드를 철거하면 인물이 옅어진다 — 깜빡임

- 화면에 실제로 보이는 양은 `인물 투명도 + 잔상 투명도 x (1 - 인물 투명도)` 다. 이 값이 떨어지면 인물이 순간 사라진 것처럼 보인다.
- 원인 두 가지.
  1. **끊긴 시점의 낮은 투명도를 잔상이 물려받았다.** 페이드 초반(예: 0.077)에 끊기면 잔상 0.077 + 새 그림 0 이라 총량이 0 에 가까워진다. → 잔상은 **항상 1 에서 시작해 사라지게** 했고, 화면에 더 많이 남아 있는 쪽 그림을 잔상으로 고르게 했다.
  2. **같은 컷으로 다시 호출되면 `else` 분기가 잔상을 즉시 숨겼다.** 새 그림이 아직 흐린데 잔상이 사라져 총량이 0.87 → 0.34 로 떨어졌다. → 진행 중이면(`characterTimer !== 0`) 그대로 두고 정리 타이머만 다시 건다.
- 결과: 등장 연출을 뺀 깜빡임 프레임 비율 **17.0% → 1.5%**, 연속 구간 10회 → 1회.
- **숨길 때 서서히 사라지게 하는 것은 시도했다가 되돌렸다.** enterScene 이 장면마다 `setCharacter(false)` 를 부르는데, 420ms 페이드아웃 뒤에 다음 장면의 페이드인이 이어지면서 인물이 옅게 보이는 시간이 두 배가 됐다(1.5% → 6.9%). 장면 전환에는 배경 크로스페이드(1초)가 함께 도니 즉시 감추는 편이 덜 눈에 띈다.

### 4. drop-shadow 두 겹이 프레임을 떨어뜨린다 — 끊김의 주범

- `.character` 에 `drop-shadow` 가 두 겹이었다. 1536x2752 알파 이미지에서 투명도가 변할 때마다 그림자를 다시 계산하고, 크로스페이드 중에는 인물과 잔상 두 장에 동시에 걸린다.
- 실측(모바일 세로, 빠르게 넘기기) — 50ms 넘게 밀린 프레임 비율

  | 조건 | 프레임 중앙 | 끊김 |
  | --- | --- | --- |
  | 그림자 2겹 | 46ms | 41% |
  | **그림자 1겹** | **34ms** | **5%** |
  | 그림자 없음 | 25ms | 2% |

- **두 번째 그림자(`0 0 1px` 흰 테두리)만 뺐다.** 실제 표시 크기에서 서브픽셀이라 눈에 띄지 않는다. 바닥 그림자는 그대로다.
- 대사창 `backdrop-filter` 도 재봤지만 영향이 작았다(41% → 27%). 그대로 둔다.
- 전체 결과: 프레임 중앙 48ms → 31ms, 끊김 44% → 6%.

## 모바일에서 인물이 움직이던 문제

- **인물의 크기·위치에는 `dvh`를 쓰지 않는다. `svh`를 쓴다.**
  - `dvh`는 주소창이 숨었다 나타날 때 값이 변하는 단위다. `.character`가 `height: 82dvh` 였고 `transition`에 `height`가 들어 있어서, 스크롤로 주소창이 접힐 때마다 인물이 0.45초 동안 스르륵 커졌다 작아졌다.
  - 실측(뷰포트 784→844): 인물 높이 642.9→692.1px(**49.2px**), 상단 10.8px 이동, 중간값 11종을 거치며 애니메이션.
  - `svh`는 주소창이 보이는 상태 기준으로 고정이라 접혀도 변하지 않는다. `.character`, `.dialogue-panel`, `.choice-panel`의 `dvh`를 전부 `svh`로 바꿨다(24곳).
  - `.app`의 `height: 100dvh`는 그대로 둔다. 화면을 꽉 채워야 하는 컨테이너라 동적인 게 맞다.
- **`.character`의 `transition`에서 `left`/`bottom`/`height`를 뺐다.** 남은 것은 `opacity`와 `filter`뿐이다.
  - 단위를 고쳐도 화면 회전 등으로 값이 바뀔 수 있다. 그때 스르륵 움직이지 않도록 즉시 반영시킨다.
  - 이 변경으로 `.character.is-swapping`이 기본 `transition`과 같아져 무의미해졌다. CSS 규칙과 JS 토글을 함께 제거했다.
- 헤드리스 브라우저에는 주소창이 없어 `svh`/`lvh`/`dvh`가 모두 같은 값이다. **`svh` 효과는 자동 테스트로 확인할 수 없고 실제 휴대폰에서만 확인된다.** 애니메이션 제거는 측정으로 확인했다(중간값 11종 → 1종).

## 대사 전환

- 대사 교체는 `#dialogue-ghost` 잔상과 크로스페이드한다(약 0.3초). 인물 컷 교체와 같은 방식이다.
  - 이전에는 `textContent`를 비우고 곧바로 타이핑을 시작해 문장이 툭 끊겼다. 배경(1초)·인물(0.52초)은 페이드하는데 글자만 즉시 바뀌어 전환 전체가 거칠게 느껴졌다.
  - `.dialogue-body` 래퍼에는 **반드시 `pointer-events: none`이 있어야 한다.** 없으면 래퍼가 클릭을 먹어 대사 영역을 탭해도 넘어가지 않는다. 아래 `.dialogue-hitbox`로 통과시켜야 한다.
  - 잔상은 `position: absolute`라 레이아웃 높이에 영향을 주지 않는다.

## 가로 폰 대사창

- 가로 폰(`max-height: 620px and orientation: landscape`)의 대사창 최소 높이는 화면에 비례시킨다: `clamp(92px, 26dvh, 140px)`.
  - 이전 고정 `160px`은 논리 높이 359px 화면에서 화면의 43%를 차지했고, 한 줄짜리 대사에도 여백만 크게 남으며 인물이 허리에서 잘렸다. 수정 후 26%.
  - `min-height`이므로 대사가 길면 늘어난다.

## Git과 Vercel

- 완성본 최초 커밋: `3ccf3d0` (`판교고 미연시 웹 게임 완성`)
- 모바일·연출 최신 커밋: `577ede7` (`모바일 연출과 캐릭터 전환 개선`)
- 원격 `main`과 `claude/annyeong-claude-0kn9ch`는 모두 `577ede7`을 가리킨다.
- Vercel이 한동안 기존 `claude/annyeong-claude-0kn9ch` 브랜치를 Production으로 추적해 두 브랜치를 함께 갱신했다.
- Vercel 설정에서 Production Branch를 `main`으로 변경하는 것이 최종 권장 상태다.
- Vercel 화면에 Source가 `3ccf3d0`으로 보이면 이전 배포 상세 페이지를 보고 있는 것이다. 크로스페이드는 `577ede7`부터 존재한다.
- 확인된 `577ede7` Production 배포:
  - `https://pangyo-dating-pt6qljpz2-willys-projects-f8ca0a2c.vercel.app`
- 확인된 `577ede7` Preview 배포:
  - `https://pangyo-dating-3t39uus3z-willys-projects-f8ca0a2c.vercel.app`
- 두 배포 모두 GitHub Deployment API에서 `success`로 확인했다.
- Vercel 계정의 Deployment Protection 여부와 Production 승격은 저장소 코드만으로 변경할 수 없다.

## 검증 기록

- `node --check js/game.js`
- `node --check script/story.js`
- `node --check script/ending.js`
- `git diff --check`
- HTML ID 중복 및 주요 DOM 연결 확인
- 시나리오: 6장면, 81대사, 4선택지
- 엔딩 도달 경로 81개 확인: good 12, normal 60, other 9
- 로컬 정적 서버에서 `/`, `/css/game.css`, `/js/game.js` 모두 HTTP 200 확인
- 새 타이틀 PNG는 RGBA이며 실제 투명 픽셀을 포함한다.

## 다음 작업 시 주의

- 작업 시작 전에 Vercel에서 실제로 보고 있는 Source 커밋을 먼저 확인한다.
- `3ccf3d0`이 아니라 최소 `577ede7` 이상이어야 모바일 위치 보정과 크로스페이드가 반영된 상태다.
- 인물 전환이 여전히 급하게 느껴질 경우 먼저 최신 배포와 캐시를 확인한다. 최신 버전에서도 문제라면 크로스페이드 시간을 0.65~0.75초로 늘리고, 크롭별 얼굴 중심을 추가 미세 조정한다.
- 사용자가 테스트한 모바일 기종과 화면 크기 또는 스크린샷이 있으면 해당 뷰포트에 맞춰 마지막 위치를 보정한다.
- `한서윤` 또는 `서윤` 문자열이 다시 들어오지 않았는지 배포 전에 검색한다.
- Vercel Production Branch가 `main`으로 바뀌기 전까지 라이브 갱신이 필요하면 두 원격 브랜치를 같은 커밋으로 유지한다.
