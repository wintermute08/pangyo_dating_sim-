# 프로젝트 메모리

최종 갱신: 2026-09-04 UTC  
저장소: `wintermute08/pangyo_dating_sim-`

이 문서는 세션이 바뀌어도 현재 결정과 작업 상태를 그대로 이어가기 위한 인수인계 기록이다. 구현과 충돌할 경우 최신 코드와 사용자의 직접 지시를 우선한다.

## 사용자 결정

- 작품은 판교고를 배경으로 하는 약 10분 분량의 학원 로맨스 비주얼노벨이다.
- 제목은 **판교고 미연시**, 부제는 **봄은, 열 걸음 늦게 온다**다.
- 히로인 이름은 **판교하**다. `한서윤`, `서윤`이라는 옛 이름을 다시 사용하지 않는다.
- 플레이어 기본 이름은 **도윤**이며 시작할 때 변경할 수 있다.
- 초기 `판교고 10분 미연시 전체 제작 기획서`의 참고 비중은 낮춘다. 최종 UI 목업과 사용자의 최신 지시를 우선한다.
- 그래픽의 현재 화풍과 전체적인 색감은 사용자가 마음에 들어 한다. 이후 수정에서도 기존 에셋과 봄·하늘색·연분홍·네이비 톤을 유지한다.
- 효과는 화면이 비어 보이지 않을 정도로만 사용하고 과하게 만들지 않는다.
- 모바일 최적화와 자연스러운 인물 전환이 중요하다.

## 현재 구현

- 별도 빌드가 필요 없는 정적 웹 게임이다. 루트의 `index.html`을 정적 서버로 제공하면 실행된다.
- 주요 파일:
  - `index.html`: 전체 화면과 모달 구조
  - `css/game.css`: 데스크톱·모바일 UI, 화면 효과, 인물 배치
  - `js/game.js`: 게임 진행, 저장, 사운드, 애니메이션과 UI 동작
  - `script/story.js`: 7개 장면(체육시간 포함), 5개 선택지
  - `script/ending.js`: 호감도와 마지막 선택에 따른 3종 엔딩
- 구현 기능:
  - 이름 입력
  - 6개 장면과 3개 엔딩
  - 자동 저장, 수동 저장 6슬롯, 불러오기
  - 대화 로그, CG 갤러리, 환경설정
  - 텍스트 속도·자동 진행·BGM·효과음 조절
  - Web Audio 기반 합성 BGM·효과음
  - 키보드 조작, 전체 화면, 모션 감소 설정
  - 반응형 모바일 세로·가로 레이아웃

## 그래픽과 타이틀

- 실제 게임 에셋은 `assets/`에 있다.
- 전달받은 원본과 비교 자료는 `docs/reference/`에 정리했다.
- 151MB짜리 `docs/reference/ui-mockup-handoff.zip`은 로컬에 보존하지만 `.gitignore`로 Git 추적에서 제외한다.
- 현재 타이틀은 `assets/title-logo-v2.png`다.
- 타이틀 v2는 기존 타이틀 참고 이미지를 생성 입력으로 사용하지 않은 독립적인 이미지 생성 결과다.
- 타이틀 방향: 현대적인 학교 노트 편집 디자인, 네이비 잉크, 하늘색 선, 열 걸음 발자국, 꽃잎 하나.
- 참고 이미지와 유사도가 높았던 첫 타이틀은 `docs/reference/title-logo-generated-v1.png`로 보관한다.
- HTML 텍스트 제목 대신 PNG 타이틀을 타이틀 화면과 스토리 타이틀 카드에서 사용한다.

## 모바일·인물 연출 상태

- 최신 관련 커밋: `577ede7` (`모바일 연출과 캐릭터 전환 개선`)
- 모바일에서 노치와 홈바 안전 영역을 `env(safe-area-inset-*)`로 반영했다.
- 모바일 대화창, 선택지, 모달, 상단 HUD, 빠른 설정 버튼의 크기와 간격을 별도 보정했다.
- 터치 버튼에는 모바일 탭 동작을 적용했고 터치 환경에서 고정되는 hover 이동을 막았다.
- 인물의 수평 중심축은 화면 방향에 따라 다르다. **세로에서는 기기와 무관하게 50%**, 가로와 데스크톱은 76%다.
  - 가로 배치(인물 오른쪽 + 왼쪽 여백에 대사)를 세로에 그대로 쓰면 인물이 오른쪽으로 잘리고 왼쪽만 크게 빈다.
    적용 전 실측: iPhone 14 중심 67%·오른쪽 64px 잘림, iPad 72%·왼쪽 319px 공백, iPad Pro 76%·왼쪽 505px 공백.
  - 세로에서는 대사창이 화면 폭을 전부 쓰므로 인물을 오른쪽으로 밀 이유가 없다.
  - 이전 값(세로 67% / 태블릿 72%)은 더 쓰지 않는다.
- 세로 분기는 `@media (orientation: portrait)` 세 단계다. 공통(수평 위치), 621px 이상 태블릿(인물 확대 + 대사 도구 균등 배치),
  901px 이상 iPad Pro급(모바일·태블릿 분기에 걸리지 않고 데스크톱 규칙을 받으므로 별도 지정).
- 전신 컷은 화면 바닥에 세우고 대화창이 다리를 덮는다.
- 허리·바스트 컷은 이미지 내 얼굴 위치가 전신 컷과 맞도록 각각 위로 들어 올린다.
- 인물 컷의 투명 캔버스 여백이 서로 크게 다르므로 단순한 동일 `right` 값으로 되돌리지 않는다.
- 표정 변경에는 `#character-ghost`와 `#character` 두 이미지를 겹치는 약 0.52초 크로스페이드를 사용한다.
- **컷 교체는 순수 크로스페이드다. 위치를 움직이지 않는다.**
  - 키프레임에서 `transform` 이동과 `scale`을 뺐다. 이전에는 들어오는 컷이 `(12px, 6px)`, 나가는 컷이 `(-8px, -2px)`로 이동해 두 이미지가 20px 어긋난 채 겹쳤다.
  - 교체 중에는 `.character.is-swapping`으로 `left`/`bottom`/`height` 트랜지션을 끈다. 컷마다 `bottom`·`height`가 달라 켜 두면 새 컷이 0.45초 동안 제자리로 미끄러져 들어온다.
  - `is-swapping`은 `src`와 `data-look`을 바꾸기 **전에** 붙여야 한다. 뒤에 붙이면 이미 트랜지션이 시작된 뒤라 소용이 없다.
  - 실측: 수정 전 데스크톱 가로 14.7px·세로 109.3px·높이 52.8px 이탈 → 수정 후 전 축 0.0px. 크로스페이드 자체는 유지된다.
  - 최초 등장 애니메이션(`character-in`)은 그대로 두었다. 등장은 아래에서 올라오는 연출이 의도된 것이다.
- 최초 등장 애니메이션과 표정 교체 애니메이션은 분리돼 있다.
- 화면 효과는 느린 햇빛 번짐, 소수의 부유 광점, 꽃잎, 모바일 배경 드리프트로 구성한다.
- 모션 감소 설정에서는 꽃잎과 광점을 숨기고 애니메이션을 사실상 비활성화한다.

## 체육시간 씬 (04)

- 씬 `04 숨을 고르는 동안`(FIFTH PERIOD)을 03과 기존 04 사이에 넣었다. 전체 7씬이 되었고 뒤 씬 번호가 하나씩 밀렸다.
- 선택지는 **호감도 0의 취향 선택지**다. 자판기 선택지와 같은 성격이며, 기존 엔딩 밸런스(도달 경로 81개 · good 12 / normal 60 / other 9)를 건드리지 않는다.
- 스탠딩 `hoodie_calm` = `assets/ch_stand_hoodie_calm.png`.
  - 실제 판교고 PGHS 회색 후드집업 사진과 기존 `ch_stand_calm.png`를 함께 레퍼런스로 넣어 **의상만** 바꾼 컷이다. 얼굴·머리·머리핀·비율·포즈가 다른 컷과 동일하다.
  - 생성 결과는 흰 배경 RGB였다. 다른 스탠딩과 맞추려고 테두리 flood fill로 배경을 빼고 RGBA로 저장했다. 인물 안쪽 흰색(티셔츠·드로우코드·양말)은 테두리와 이어져 있지 않아 그대로 남는다.
  - **테두리 flood fill 만으로는 머리카락에 둘러싸인 배경 조각이 남는다.** 옆머리와 어깨 사이의 얇은 흰 띠가 그랬다. 두 기준을 함께 써서 따로 지웠다.
    1. 조각 바깥 한 겹이 대부분 어두운가 → 머리카락에 둘러싸였다
    2. 실루엣 바깥(투명)까지 거리가 70px 미만인가 → 가장자리의 얇은 틈이다
    드로우코드도 1번은 만족하지만(어두운 외곽선과 초록 글자에 접함) 몸 한가운데라 2번에서 걸러진다. 실측: 머리 틈 78%/4px, 드로우코드 64~66%/101~195px.
    조건 하나만 쓰면 드로우코드가 지워진다. 반드시 둘을 같이 봐야 한다.
  - 프레이밍 검증: 가로중심 49.3%(기존 48.6%), 머리 위 여백 4.9%(기존과 동일).
  - 명찰은 `판교하`다. 실물 사진의 명찰은 실존 인물 이름이라 쓰지 않는다.
  - **생성 모델이 명찰 한글을 `만고하`로 잘못 그렸다.** 스프라이트를 다시 생성하면 얼굴이 달라지므로 `tools/relabel_name_patch.py`로 명찰 안쪽 글자만 덮어썼다. 테두리와 알파는 건드리지 않는다. 변경 범위는 1,368~1,485px 이고 알파 변경은 0px 이다.
  - 후처리 스크립트는 `tools/`에 있다. 앞으로 후드 컷을 다시 만들면 같은 두 단계를 거쳐야 한다.
  - 신발은 흰 실내화다. 처음 생성물은 교복 로퍼였는데 체육관 바닥과 맞지 않아 같은 컷을 기준 이미지로 다시 넣어 신발만 바꿨다.
- 스탠딩 `hoodie_smile` = `assets/ch_stand_hoodie_smile.png`.
  - `hoodie_calm`(신발 수정본)을 기준 이미지로 삼아 **표정만** 바꿨다. 정렬 오차는 최대 18px(세로 2752px 기준 0.65%)이라 크로스페이드에서 어긋나 보이지 않는다.
  - 두 컷 모두 `crop: 'full'`로 통일했다. 프레이밍이 같아야 컷 교체 시 위치가 흔들리지 않는다.
  - 씬 04의 표정 흐름: calm → calm → smile → calm → smile → smile → calm → smile.
- 배경 `bg06_gym_afternoon.png`는 **임시 생성본**이다. 사용자가 실제 체육관을 촬영해 AI 변환한 이미지로 교체할 예정이다. 파일명을 그대로 덮어쓰면 된다.

## 대사 전환

- 대사 교체는 `#dialogue-ghost` 잔상과 크로스페이드한다(약 0.3초). 인물 컷 교체와 같은 방식이다.
  - 이전에는 `textContent`를 비우고 곧바로 타이핑을 시작해 문장이 툭 끊겼다. 배경(1초)·인물(0.52초)은 페이드하는데 글자만 즉시 바뀌어 전환 전체가 거칠게 느껴졌다.
  - `.dialogue-body` 래퍼에는 **반드시 `pointer-events: none`이 있어야 한다.** 없으면 래퍼가 클릭을 먹어 대사 영역을 탭해도 넘어가지 않는다. 아래 `.dialogue-hitbox`로 통과시켜야 한다.
  - 잔상은 `position: absolute`라 레이아웃 높이에 영향을 주지 않는다.

## 가로 폰 대사창

- 가로 폰(`max-height: 620px and orientation: landscape`)의 대사창 최소 높이는 화면에 비례시킨다: `clamp(92px, 26dvh, 140px)`.
  - 이전 고정 `160px`은 논리 높이 359px 화면에서 화면의 43%를 차지했고, 한 줄짜리 대사에도 여백만 크게 남으며 인물이 허리에서 잘렸다. 수정 후 26%.
  - `min-height`이므로 대사가 길면 늘어난다.

## Git과 Vercel

- 완성본 최초 커밋: `3ccf3d0` (`판교고 미연시 웹 게임 완성`)
- 모바일·연출 최신 커밋: `577ede7` (`모바일 연출과 캐릭터 전환 개선`)
- 원격 `main`과 `claude/annyeong-claude-0kn9ch`는 모두 `577ede7`을 가리킨다.
- Vercel이 한동안 기존 `claude/annyeong-claude-0kn9ch` 브랜치를 Production으로 추적해 두 브랜치를 함께 갱신했다.
- Vercel 설정에서 Production Branch를 `main`으로 변경하는 것이 최종 권장 상태다.
- Vercel 화면에 Source가 `3ccf3d0`으로 보이면 이전 배포 상세 페이지를 보고 있는 것이다. 크로스페이드는 `577ede7`부터 존재한다.
- 확인된 `577ede7` Production 배포:
  - `https://pangyo-dating-pt6qljpz2-willys-projects-f8ca0a2c.vercel.app`
- 확인된 `577ede7` Preview 배포:
  - `https://pangyo-dating-3t39uus3z-willys-projects-f8ca0a2c.vercel.app`
- 두 배포 모두 GitHub Deployment API에서 `success`로 확인했다.
- Vercel 계정의 Deployment Protection 여부와 Production 승격은 저장소 코드만으로 변경할 수 없다.

## 검증 기록

- `node --check js/game.js`
- `node --check script/story.js`
- `node --check script/ending.js`
- `git diff --check`
- HTML ID 중복 및 주요 DOM 연결 확인
- 시나리오: 6장면, 81대사, 4선택지
- 엔딩 도달 경로 81개 확인: good 12, normal 60, other 9
- 로컬 정적 서버에서 `/`, `/css/game.css`, `/js/game.js` 모두 HTTP 200 확인
- 새 타이틀 PNG는 RGBA이며 실제 투명 픽셀을 포함한다.

## 다음 작업 시 주의

- 작업 시작 전에 Vercel에서 실제로 보고 있는 Source 커밋을 먼저 확인한다.
- `3ccf3d0`이 아니라 최소 `577ede7` 이상이어야 모바일 위치 보정과 크로스페이드가 반영된 상태다.
- 인물 전환이 여전히 급하게 느껴질 경우 먼저 최신 배포와 캐시를 확인한다. 최신 버전에서도 문제라면 크로스페이드 시간을 0.65~0.75초로 늘리고, 크롭별 얼굴 중심을 추가 미세 조정한다.
- 사용자가 테스트한 모바일 기종과 화면 크기 또는 스크린샷이 있으면 해당 뷰포트에 맞춰 마지막 위치를 보정한다.
- `한서윤` 또는 `서윤` 문자열이 다시 들어오지 않았는지 배포 전에 검색한다.
- Vercel Production Branch가 `main`으로 바뀌기 전까지 라이브 갱신이 필요하면 두 원격 브랜치를 같은 커밋으로 유지한다.
